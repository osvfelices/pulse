// For loops
for (let i = 0; i < 10; i++) {
  x = i
}

for (const item of items) {
  process(item)
}

for (const key in obj) {
  print(key)
}

for await (const item of asyncIterable) {
  process(item)
}
