// String utilities module
export fn greet(name) {
  return "Hello, " + name + "!"
}

export fn uppercase(str) {
  return str.toUpperCase()
}

export fn repeat(str, n) {
  let result = ""
  for (let i = 0; i < n; i++) {
    result = result + str
  }
  return result
}
